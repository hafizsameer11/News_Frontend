/**
 * Base Controller
 * Empty abstract example for controller structure
 *
 * Controllers should:
 * - Have NO business logic
 * - Call services only
 * - Return responses using successResponse/errorResponse
 */
export abstract class BaseController {
  // Add common controller methods here if needed
}
