/**
 * Base Service
 * Empty abstract example for service structure
 *
 * Services should:
 * - Implement application logic
 * - Throw errors if required
 * - Call repositories
 */
export abstract class BaseService {
  // Add common service methods here if needed
}
